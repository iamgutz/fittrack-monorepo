{
  "name": "fittrack-monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run open && docker-compose up --build",
    "open": "open http://localhost:5173",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
